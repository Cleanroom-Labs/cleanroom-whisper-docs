# Sphinx and extensions for Cleanroom Whisper documentation
# Keep in sync with the aggregator baseline in cleanroom-technical-docs/requirements.txt

sphinx>=7.0.0,<8.0.0
sphinx-rtd-theme>=3.0.0
sphinx-needs>=6.3.0
myst-parser>=3.0.0
graphviz>=0.20.0
sphinxcontrib-rust>=1.0.0

# Project-specific dependencies (if any)
# Add additional packages here as needed
